<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Devicely | Trade used electronic devices</title>
    <link rel="stylesheet" href="/css/base.css" />
    <link rel="stylesheet" href="/css/index.css" />
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body>
    <section class="todoapp">
      <nav class="nav">
        <ul>
          <li class="user"><%= user.name || user.username || user.email %></li>
          <li>
            <form action="/mydevices" method="get">
              <button class="" type="submit">My Devices</button>
            </form>
          </li>
          <li>
            <form action="/logout" method="post">
              <button class="logout" type="submit">Sign out</button>
            </form>
          </li>
        </ul>
      </nav>
      <header class="header">
        <h1>Devicely</h1>
      </header>
    </section>
    <section>
      <div class="devicesList">
        <% for (let i = 0; i < myDevices.length; i++){ %>
        <div class="devicesCard">
          <video width="250" height="250" controls name="media">
            <source src="<%=myDevices[i].videoPath %>" type="video/mp4" />
          </video>
          <h4 class="text-align-center"><%=myDevices[i].name %></h4>
          <button class="btn success">EDIT</button>
          <hr />
        </div>
        <% } %>
      </div>
    </section>
    <footer class="info">
      <p>
        Created by
        <a href="https://github.com/StrawberryChocolateFudge"
          >StrawberryChocolateFudge</a
        >
      </p>
      <p>For the Sustainable Blockchain Hackathon</p>
    </footer>
  </body>
</html>
